# lscd
ディレクトリチェンジャー

カレントディレクトリのサブディレクトリをメニュー表示し、選択したディレクトリに移動して終了するシェルスクリプト製のコマンドです。
メニュー表示にはwhiptailが使用されています。

```bash
ls --help
Usage: lscd [ OPTION ]

  -h, --help       使い方を表示して終了する
  -v, --version    バージョン情報を表示して終了する
```
